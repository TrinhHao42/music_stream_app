export type AlbumEmbedded = {
  albumId: string;
  albumName: string;
  image?: string;
};

export type SongEmbedded = {
  songId: string;
  title: string;
  coverUrl?: string;
};

export default {};


